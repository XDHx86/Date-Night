# Bun overrides the global node_modules layout by default. Keeping the
# 24h guard protects against accidentally shipping recently-published
# packages that have not been seen by the supply-chain tool yet.
[install]
minimumReleaseAge = 86400
